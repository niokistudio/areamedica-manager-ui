import { heroui } from "@heroui/theme"

export default heroui({
  defaultTheme: "light",
  themes: {
    light: {
      colors: {
        primary: {
          DEFAULT: "#26307c",
          foreground: "#fafafa",
        },
        foreground: "#343c3d",
      },
    },
  },
})
